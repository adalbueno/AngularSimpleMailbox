(function(angular) {
  'use strict';

  var dependencies = [
    // Angular modules
    'ngRoute',

    // Custom modules
    'mailbox'
  ];

  angular.module('app', dependencies);

})(window.angular);
